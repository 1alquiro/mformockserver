/**
 * Generates one instance of coursesInfo object
 * @param {number} id Identifier of coursesInfo object instance
 * @returns {Object}
 */
exports.coursesInfoData = function(id) {
	return {"link": "http://www.google.es/search?q=curso" + id}
}